
#include <iostream>
#include <vector>
#include <tuple>
#include <map>
#include <set>

int main() {

  using namespace std;

  map<string, vector<string>> G;

  string line;
  set<tuple<string, vector<string>, int>> bag;
  while (cin >> line) {
  }

  vector<string> v;
  auto tp = make_tuple(line, v, 0);

  return 0;
}
